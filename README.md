# Factory App Bolt Package Build System

Build scripts for assembling, packaging, signing, and generating manifests for RDK-E factory app bolt packages.

# Important Notice

These applications are signed using a test key. Never use the public keys and binaries in production!
